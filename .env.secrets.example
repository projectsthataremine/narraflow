# Supabase Edge Function Secrets
# Copy this file to .env.secrets and fill in your actual values
# DO NOT COMMIT .env.secrets - it's in .gitignore

# Stripe (Production - Live Mode)
STRIPE_SECRET_KEY_PROD=sk_live_xxxxx
STRIPE_PRICE_ID_PROD=price_xxxxx  # Monthly or annual price ID from Stripe
STRIPE_WEBHOOK_SECRET_PROD=whsec_xxxxx  # From Stripe webhook endpoint

# Stripe (Sandbox - Test Mode)
STRIPE_SECRET_KEY_SANDBOX=sk_test_xxxxx
STRIPE_PRICE_ID_SANDBOX=price_xxxxx  # Test mode price ID
STRIPE_WEBHOOK_SECRET_SANDBOX=whsec_xxxxx  # From test mode webhook endpoint

# Licensing (shared between dev and prod)
LICENSE_PRIVATE_KEY=base64_encoded_private_key  # Ed25519 private key, base64 encoded
LICENSE_PUBLIC_KEY=base64_encoded_public_key  # Ed25519 public key, base64 encoded
EDGE_FUNCTION_SECRET=random_secret_string  # Generate with: openssl rand -base64 32

# After filling in values, upload to Supabase with:
# ./scripts/upload-secrets.sh
